#!/bin/bash
# -*- mode: sh -*-
{% if args.job_name -%}
#$ -N {{args.job_name}}
{%- endif %}
{% if args.queue -%}
#$ -q {{args.queue}}
{%- endif %}
{% if args.email -%}
#$ -M {{args.email}} -m aes
{%- endif %}
#$ -S /bin/bash
#$ -cwd
#$ -o {{args.sge_dir}}
#$ -j y
#$ -V
#$ -l h_vmem={{args.memory}}
#$ -l h_cpu={{args.time}}
#$ -pe smp {{args.cpus}}

export OMP_NUM_THREADS={{args.cpus}}
{{args.command}}
